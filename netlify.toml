[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA-style: this is a single page, but keep a safe fallback.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
